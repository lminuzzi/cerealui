import { PedidoDadoBancario } from './pedido-dado-bancario';

describe('PedidoDadoBancario', () => {
  it('should create an instance', () => {
    expect(new PedidoDadoBancario()).toBeTruthy();
  });
});
